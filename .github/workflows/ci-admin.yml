name: CI - aha! admin

on:
  push:
    branches: [main, dev]
    paths:
      - 'apps/admin/**'
      - 'packages/shared/**'
  pull_request:
    branches: [main, dev]
    paths:
      - 'apps/admin/**'
      - 'packages/shared/**'

jobs:
  ci:
    name: Lint & Build
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: apps/admin

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
        env:
          VITE_APP_NAME: ${{ vars.VITE_APP_NAME || 'aha! admin' }}
          VITE_APP_ENV: ${{ vars.VITE_APP_ENV || 'production' }}
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
          VITE_ADMIN_SECRET: ${{ secrets.VITE_ADMIN_SECRET }}
